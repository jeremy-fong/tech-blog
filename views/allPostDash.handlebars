<div id="postDash" class="posts list-group">
  {{#each posts as |post|}}
    <a id="postList" class="list-item flex-row justify-space-between" href="/dashboard/edit/{{id}}">
      <span>{{title}}</span>
      <span>{{format_date dateCreated}}</span>
    </a>
  {{/each}}
</div>